package com.test.demo.caseTest.streamTest;

public class Test2 {
    public static void main(String[] args) {
        /*Integer[] integerArr = {2, 3, 4, 5, 6, 7, 8};
        List<Integer> integerList1 = Arrays.asList(1, 1, 1, 2, 3, 4, 5, 5, 6, 7, 8, 9, 9);
        List<Integer> integerList = Arrays.asList(integerArr);
        List<List<Integer>> collect1 = integerList.stream().map(a -> {
            List<Integer> collect = integerList1.stream().filter(b -> a.equals(b)).collect(Collectors.toList());
            return collect;
        }).collect(Collectors.toList());
        System.out.println(collect1);*/

    }
}
