spring:
  profiles:
    active: prod
  cloud:
    nacos:
      config:
        file-extension: yml
        #各个微服务共享的配置，注意越排到后面的公共配置yml优先级越高
        shared-dataids: discovery-common.yml
        group: base
  application:
    name: demo

---
#生产环境配置
spring:
  profiles: dev
  cloud:
    nacos:
      config:
        server-addr: 121.36.128.146:8848
        # iot-platform
        namespace: d866aaaa-d339-4909-a2bf-40d9878e73f1
---

#开发环境配置
spring:
  profiles: prod
  cloud:
    nacos:
      config:
        server-addr: 172.20.97.225:8848
        # iot-platform
        namespace: 74f9e713-818c-49f9-a0cd-580af3a89f4e
